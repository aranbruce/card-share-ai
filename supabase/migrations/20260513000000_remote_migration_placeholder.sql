-- This version appears in the linked Supabase project's migration history but
-- the original SQL was not in the repo. Placeholder keeps `supabase db push`
-- and migration list in sync. Safe no-op (SELECT 1).
SELECT 1;

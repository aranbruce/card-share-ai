/** Supabase `.select()` list for `card_contributions` rows exposed in JSON (excludes `edit_token`). */
export const CONTRIBUTION_PUBLIC_COLUMNS =
  'id, card_id, message, is_creator, created_at, position_x, position_y, width_percent, page_index, font_size'
